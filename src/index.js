import React from "react";
import ReactDOM from "react-dom/client";
import PageCampagne from "./PageCampagne";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <PageCampagne />
  </React.StrictMode>
);
